# -*- coding: utf-8 -*-
# def resetPoint(treeIdx, talentCount):
#     """
#     重置天赋点数
#     @param treeIdx:
#     @param talentCount:
#     @return:
#     """
#     treePoint, nodeList = talent.findTreeAndPoint(treeIdx)
#     for n in nodeList:
#         node, totalPoints = talent.findParentAndPoint(treeIdx, n)
#         node.talentNum = 0
#     talentCount += treePoint
# a = [1, 3, 4, 7, 9]
# l = len(a)
# for i in range(0, l):
#     a[i] = 0
# print(a)
#
# # for k, v in datas.items():
# #     node_lists = [0 if i > 0 else i for i in list(v['relation'].keys())]
# #     print(node_lists)
a = [1, 2, 3, 4]
b = 0
for i in a:
    print(i, b)
print(("%s—%s" % (a, b)), 1)
print("%s—%s" % (a, b), 1)
for i in a:
    print(i)
    i = 0
    print(i)
#
# f = eval("{'i': a, 'w': a}")
# for i in a:
#     g = i
#     d = eval("{'i': g}")
#     print(d)

#
# for n in range(0, len(nodeList)):
#     node = nodeList[n]
#     talentNum = node.talentNum = 0
#     talent_result.append(NodeTalentNum().createFromDict(talentObj))
